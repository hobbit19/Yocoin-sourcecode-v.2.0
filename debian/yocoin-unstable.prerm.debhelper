# Automatically added by dh_systemd_start/11.3.5
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop 'yocoin-unstable.service' >/dev/null || true
fi
# End automatically added section
